<%= simple_form_for(task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% title = (task.created_at.nil? ? 'Create a new task' : 'Update task') %>
  <%= f.button :submit, title, class: "btn-gradient" %>
<% end %>
